{
  "name": "sprintbrain-mcp-memory",
  "version": "3.11.0",
  "private": true,
  "type": "module",
  "description": "SprintBrain working-memory MCP server. Attaches and detaches memory shards per step, inside a token budget.",
  "main": "src/index.ts",
  "bin": {
    "sprintbrain-mcp-memory": "src/index.ts"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
