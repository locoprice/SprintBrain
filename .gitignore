# Dependencies
node_modules/

# Sensitive — Chrome extension signing key. NEVER commit.
*.pem
sb-extension.pem

# Build output
dist/

# OS / editor
.DS_Store
Thumbs.db

# Claude Code local config — never commit
.claude/

# Extension build artifacts (sprintbrain_vX.Y.Z.zip from packaging)
*.zip
