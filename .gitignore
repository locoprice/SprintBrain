# Dependencies
node_modules/

# Sensitive — Chrome extension signing key. NEVER commit.
*.pem
sb-extension.pem

# Build output
dist/

# OS / editor
.DS_Store
Thumbs.db

# Claude Code local config — never commit, except the shared team skills
.claude/*
!.claude/skills/

# Session scratch — multi-session task progress, never commit
_progress.md

# MCP client config — carries the memory server's personal access token. Never commit.
.mcp.json

# Extension build artifacts (sprintbrain_vX.Y.Z.zip from packaging)
*.zip
